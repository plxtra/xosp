{
	"Services": {
		"XOSP": {
			"Url": "http://sessions/",
			"Credentials": "XOSP"
		}
	},
	"Credentials": {
		"XOSP": {
			"Passport": "https://auth.${HttpsUri}/",
			"ClientID": "${ClientID-SessionsControl}",
			"ClientSecret": "${ClientSecret-SessionsControl}",
			"Scopes": [ "Sessions${AuthSuffix}/Admin" ]
		}
	}
}