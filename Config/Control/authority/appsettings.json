{
	"Services": {
		"XOSP": {
			"Url": "http://authority/",
			"Credentials": "XOSP"
		}
	},
	"Credentials": {
		"XOSP": {
			"Url": "https://auth.${RootUri}/",
			"ClientID": "${ClientID-AuthorityControl}",
			"ClientSecret": "${ClientSecret-AuthorityControl}",
			"Scopes": [ "Authority${AuthSuffix}/Admin" ]
		}
	}
}